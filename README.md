# Certificreate

Fresh Next.js app for Certificreate.

> TODO: Fill in the product purpose in `blueprint/project-plan.md`, then run
> `/overview` or `$overview`.

## Commands

- `npm run dev` - start the dev server at http://localhost:3000
- `npm run build` - build the production app with Next.js webpack
- `npm run start` - start the production server
- `npm run lint` - run ESLint

No test command is configured yet. Run `/tests` or `$tests` when you want to add
unit testing through the Blueprint workflow.
